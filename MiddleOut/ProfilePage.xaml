<UserControl x:Class="MiddleOut.ProfilePage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mui="http://firstfloorsoftware.com/ModernUI"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Style="{StaticResource ContentRoot}">
        <Grid.RowDefinitions>
            <RowDefinition Height="6*"/>
            <RowDefinition Height="4*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" MinWidth="200">
            <TextBlock Text="Profile" Style="{StaticResource Heading2}" />
        </StackPanel>

        <ScrollViewer>
            <StackPanel Grid.Row="1" MinWidth="200">
                <TextBlock Text="Reports" Style="{StaticResource Heading2}" FontSize="12" HorizontalAlignment="Left"/>
                <TextBlock x:Name="textBlock" TextWrapping="Wrap" MinHeight="85" HorizontalAlignment="Stretch"/>

            </StackPanel>
        </ScrollViewer>


    </Grid>
</UserControl>
